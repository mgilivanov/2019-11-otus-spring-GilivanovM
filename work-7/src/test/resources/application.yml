locale:
  language: en
  country: US
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    data: test_data.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true

logging:
  level:
    ROOT: ERROR